import 'package:flutter/material.dart';
class CurveEDges extends StatelessWidget {
  const CurveEDges({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
